/// Marca da instalação, injetada pelo `brandPlugin` (vite) e pelo
/// `vitest.config.ts`. O formato é `AppBrand`, em `config/company.ts`.
declare const __APP_BRAND__: import('./config/company').AppBrand;
